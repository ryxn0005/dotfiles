#!/bin/sh

wallpaper=$(ls ~/Pictures | wmenu -i -f "JetBrainsMono Nerd Font Bold 16" -l 5)

[ -z "$wallpaper" ] && exit

killall swaybg 2>/dev/null
echo "$HOME/Pictures/$wallpaper" > ~/.cache/current_wallpaper
swaybg --output '*' --mode fill --image "$HOME/Pictures/$wallpaper" &
